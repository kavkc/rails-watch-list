<div class="container">
  <h2>Create new bookmark</h2>
  <%= simple_form_for [@list, @bookmark] do |f| %>
    <%= f.input :comment %>
    <%= f.association :movie %>
    <p class= "btn btn-primary"><%= f.button :submit %></p>
  <% end %>
</div>
